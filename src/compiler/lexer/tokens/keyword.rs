#[derive(Clone, Debug, PartialEq)]
pub enum Keyword {
    Import,
    From,
    Pub,
    Fn,
    Const,
    Let,
    Mut,
    Struct,
    Class,
    Enum,
    Errors,
    Self_,
    Construct,
    Return,
    If,
    Else,
    Loop,
    While,
    For,
    In,
}

